<Window x:Class="TLEditor.TableInsertDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Insert Table" Height="350" Width="450"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <TextBlock Grid.Row="0" Text="Number of Rows:" Margin="0,0,0,8" FontSize="14" FontWeight="Medium"/>
        <TextBox Grid.Row="1" Name="RowsTextBox" Text="3" Margin="0,0,0,25" Width="150" Height="32" HorizontalAlignment="Left" FontSize="14"/>
        
        <TextBlock Grid.Row="2" Text="Number of Columns:" Margin="0,0,0,8" FontSize="14" FontWeight="Medium"/>
        <TextBox Grid.Row="3" Name="ColumnsTextBox" Text="3" Margin="0,0,0,25" Width="150" Height="32" HorizontalAlignment="Left" FontSize="14"/>
        
        <!-- Spacer row -->
        <Border Grid.Row="4" Height="20"/>
        
        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,20">
            <Button Name="OKButton" Content="OK" Width="100" Height="35" Margin="0,0,20,0" Click="OKButton_Click" IsDefault="True" FontSize="14"/>
            <Button Name="CancelButton" Content="Cancel" Width="100" Height="35" Click="CancelButton_Click" IsCancel="True" FontSize="14"/>
        </StackPanel>
    </Grid>
</Window>